# Terraform placeholders for later cloud deployment.
# Do not implement full production infrastructure in Phase 1.

terraform {
  required_version = ">= 1.5.0"
}

# Future modules: networking, Postgres, Redis, container runtime, secrets.

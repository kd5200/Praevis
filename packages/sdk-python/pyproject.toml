[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "praevis-sdk"
version = "0.1.0"
description = "Praevis Python SDK (stub)"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/praevis_sdk"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""

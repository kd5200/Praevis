# Security rules package

Versioned, deterministic detector rules for prompt-injection and related patterns.

Phase 1: package layout and empty rule catalog only.
Phase 2: YAML/JSON rules consumed by the analysis pipeline.

Rules must remain reviewable without an LLM.

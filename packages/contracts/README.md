# Shared contracts

OpenAPI-aligned and domain shared types will live here.
Phase 1: placeholder only — avoid duplicating models across apps.

Future contents:

- Scan / Finding / RetrievedContent schemas
- Decision and status enums
- JSON Schema / OpenAPI fragments exported to SDKs
